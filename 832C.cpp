#include<bits/stdc++.h>
using namespace std;

int main(){
	int n,s;
	cin>>n>>s;

	int x,v,t;
	vector<pair<int,int> > p1,p2,p;
	for(int i=0;i<n;i++){
		cin>>x>>v>>t;
		if(t==1){p1.push_back(make_pair(x,v));}
		else{p1.push_back(make_pair(x,v));}
		p.push_back(make_pair(x,v));
	}
	sort(p1.begin(),p1.end());
	sort(p2.begin(),p2.end());
	x=p1.size(),y=p2,size();

	
	
}